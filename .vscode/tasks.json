{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "type": "shell",
  "tasks": [
    {
      "label": "build",
      "command": "cd cfi-test && make",
      "problemMatcher": [
        "$gcc"
      ]
    },
    {
      "label": "make out",
      "command": "cd cfi-test && make out",
      "problemMatcher": [
        "$gcc"
      ]
    }
  ]
}